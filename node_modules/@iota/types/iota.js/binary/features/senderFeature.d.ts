import type { ReadStream, WriteStream } from "../../../util.js";
import { ISenderFeature } from "../../models/features/ISenderFeature";
/**
 * The minimum length of a sender feature binary representation.
 */
export declare const MIN_SENDER_FEATURE_LENGTH: number;
/**
 * Deserialize the sender feature from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeSenderFeature(readStream: ReadStream): ISenderFeature;
/**
 * Serialize the sender feature to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeSenderFeature(writeStream: WriteStream, object: ISenderFeature): void;
