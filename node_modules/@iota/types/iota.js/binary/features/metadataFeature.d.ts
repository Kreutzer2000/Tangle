import { ReadStream, WriteStream } from "../../../util.js";
import { IMetadataFeature } from "../../models/features/IMetadataFeature";
/**
 * The minimum length of a metadata feature block binary representation.
 */
export declare const MIN_METADATA_FEATURE_LENGTH: number;
/**
 * Deserialize the metadata feature block from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeMetadataFeature(readStream: ReadStream): IMetadataFeature;
/**
 * Serialize the metadata feature to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeMetadataFeature(writeStream: WriteStream, object: IMetadataFeature): void;
