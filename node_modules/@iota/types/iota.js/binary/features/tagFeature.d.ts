import { ReadStream, WriteStream } from "../../../util.js";
import { ITagFeature } from "../../models/features/ITagFeature";
/**
 * The minimum length of a tag feature binary representation.
 */
export declare const MIN_TAG_FEATURE_LENGTH: number;
/**
 * Deserialize the tag feature from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeTagFeature(readStream: ReadStream): ITagFeature;
/**
 * Serialize the tag feature to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeTagFeature(writeStream: WriteStream, object: ITagFeature): void;
