import type { ReadStream, WriteStream } from "../../../util.js";
import { IIssuerFeature } from "../../models/features/IIssuerFeature";
/**
 * The minimum length of a issuer feature binary representation.
 */
export declare const MIN_ISSUER_FEATURE_LENGTH: number;
/**
 * Deserialize the issuer feature from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeIssuerFeature(readStream: ReadStream): IIssuerFeature;
/**
 * Serialize the issuer feature to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeIssuerFeature(writeStream: WriteStream, object: IIssuerFeature): void;
