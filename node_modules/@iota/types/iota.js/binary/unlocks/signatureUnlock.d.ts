import type { ReadStream, WriteStream } from "../../../util.js";
import { ISignatureUnlock } from "../../models/unlocks/ISignatureUnlock";
/**
 * The minimum length of a signature unlock binary representation.
 */
export declare const MIN_SIGNATURE_UNLOCK_LENGTH: number;
/**
 * Deserialize the signature unlock from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeSignatureUnlock(readStream: ReadStream): ISignatureUnlock;
/**
 * Serialize the signature unlock to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeSignatureUnlock(writeStream: WriteStream, object: ISignatureUnlock): void;
