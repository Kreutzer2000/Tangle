import type { ReadStream, WriteStream } from "../../../util.js";
import { IAliasUnlock } from "../../models/unlocks/IAliasUnlock";
/**
 * The minimum length of a alias unlock binary representation.
 */
export declare const MIN_ALIAS_UNLOCK_LENGTH: number;
/**
 * Deserialize the alias unlock from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeAliasUnlock(readStream: ReadStream): IAliasUnlock;
/**
 * Serialize the alias unlock to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeAliasUnlock(writeStream: WriteStream, object: IAliasUnlock): void;
