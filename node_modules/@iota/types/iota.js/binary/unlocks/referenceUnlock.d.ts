import type { ReadStream, WriteStream } from "../../../util.js";
import { IReferenceUnlock } from "../../models/unlocks/IReferenceUnlock";
/**
 * The minimum length of a reference unlock binary representation.
 */
export declare const MIN_REFERENCE_UNLOCK_LENGTH: number;
/**
 * Deserialize the reference unlock from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeReferenceUnlock(readStream: ReadStream): IReferenceUnlock;
/**
 * Serialize the reference unlock to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeReferenceUnlock(writeStream: WriteStream, object: IReferenceUnlock): void;
