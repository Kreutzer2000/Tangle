import type { ReadStream, WriteStream } from "../../../util.js";
import type { UnlockTypes } from "../../models/unlocks/unlockTypes";
/**
 * The minimum length of an unlock binary representation.
 */
export declare const MIN_UNLOCK_LENGTH: number;
/**
 * Deserialize the unlocks from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeUnlocks(readStream: ReadStream): UnlockTypes[];
/**
 * Serialize the unlocks to binary.
 * @param writeStream The stream to write the data to.
 * @param objects The objects to serialize.
 */
export declare function serializeUnlocks(writeStream: WriteStream, objects: UnlockTypes[]): void;
/**
 * Deserialize the unlock from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeUnlock(readStream: ReadStream): UnlockTypes;
/**
 * Serialize the unlock to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeUnlock(writeStream: WriteStream, object: UnlockTypes): void;
