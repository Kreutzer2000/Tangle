import type { ReadStream, WriteStream } from "../../../util.js";
import { INftUnlock } from "../../models/unlocks/INftUnlock";
/**
 * The minimum length of a nft unlock binary representation.
 */
export declare const MIN_NFT_UNLOCK_LENGTH: number;
/**
 * Deserialize the nft unlock from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeNftUnlock(readStream: ReadStream): INftUnlock;
/**
 * Serialize the nft unlock to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeNftUnlock(writeStream: WriteStream, object: INftUnlock): void;
