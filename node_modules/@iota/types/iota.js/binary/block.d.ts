import type { ReadStream, WriteStream } from "../../util.js";
import { IBlock } from "../models/IBlock";
/**
 * The maximum length of a block.
 */
export declare const MAX_BLOCK_LENGTH: number;
/**
 * The maximum number of parents.
 */
export declare const MAX_NUMBER_PARENTS: number;
/**
 * The minimum number of parents.
 */
export declare const MIN_NUMBER_PARENTS: number;
/**
 * Deserialize the block from binary.
 * @param readStream The block to deserialize.
 * @returns The deserialized block.
 */
export declare function deserializeBlock(readStream: ReadStream): IBlock;
/**
 * Serialize the block essence to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeBlock(writeStream: WriteStream, object: IBlock): void;
