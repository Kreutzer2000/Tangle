import type { ReadStream, WriteStream } from "../../../util.js";
import type { ITypeBase } from "../../models/ITypeBase";
import type { MilestoneOptionTypes } from "../../models/milestoneOptions/milestoneOptionTypes";
/**
 * The minimum length of a milestone option binary representation.
 */
export declare const MIN_MILESTONE_OPTION_LENGTH: number;
/**
 * Deserialize the milestone options from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeMilestoneOptions(readStream: ReadStream): MilestoneOptionTypes[];
/**
 * Serialize the milestone options to binary.
 * @param writeStream The stream to write the data to.
 * @param objects The objects to serialize.
 */
export declare function serializeMilestoneOptions(writeStream: WriteStream, objects: MilestoneOptionTypes[]): void;
/**
 * Deserialize the milestone options from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeMilestoneOption(readStream: ReadStream): MilestoneOptionTypes;
/**
 * Serialize the milestone option to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeMilestoneOption(writeStream: WriteStream, object: ITypeBase<number>): void;
