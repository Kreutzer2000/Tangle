import type { ReadStream, WriteStream } from "../../../util.js";
import { IReceiptMilestoneOption } from "../../models/milestoneOptions/IReceiptMilestoneOption";
/**
 * The minimum length of a receipt milestone option binary representation.
 */
export declare const MIN_RECEIPT_MILESTONE_OPTION_LENGTH: number;
/**
 * Deserialize the receipt milestone option from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeReceiptMilestoneOption(readStream: ReadStream): IReceiptMilestoneOption;
/**
 * Serialize the receipt milestone option to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeReceiptMilestoneOption(writeStream: WriteStream, object: IReceiptMilestoneOption): void;
