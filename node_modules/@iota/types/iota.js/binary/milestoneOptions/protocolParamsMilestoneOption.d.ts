import { ReadStream, WriteStream } from "../../../util.js";
import { IProtocolParamsMilestoneOption } from "../../models/milestoneOptions/IProtocolParamsMilestoneOption";
/**
 * The minimum length of a protocol params milestone option binary representation.
 */
export declare const MIN_PROTOCOL_PARAMS_MILESTONE_OPTION_LENGTH: number;
/**
 * Deserialize the protocol params milestone option from binary.
 * @param readStream The stream to read the data from.
 * @returns The deserialized object.
 */
export declare function deserializeProtocolParamsMilestoneOption(readStream: ReadStream): IProtocolParamsMilestoneOption;
/**
 * Serialize the protocol params milestone option to binary.
 * @param writeStream The stream to write the data to.
 * @param object The object to serialize.
 */
export declare function serializeProtocolParamsMilestoneOption(writeStream: WriteStream, object: IProtocolParamsMilestoneOption): void;
