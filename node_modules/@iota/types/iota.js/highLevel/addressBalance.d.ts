import { BigInteger } from "../../big-int";
import type { IClient } from "../models/IClient";
/**
 * Get the balance for an address.
 * @param client The client or node endpoint to get the information from.
 * @param addressBech32 The address to get the balances for.
 * @returns The balance.
 */
export declare function addressBalance(client: IClient | string, addressBech32: string): Promise<{
    balance: BigInteger;
    nativeTokens: {
        [id: string]: BigInteger;
    };
    ledgerIndex: number;
}>;
