import * as Promise from 'bluebird';
import { Callback, Provider } from '../../types';
/**
 * @method createStoreAndBroadcast
 *
 * @memberof module:core
 *
 * @ignore
 *
 * @param {Provider} provider
 *
 * @return {function} {@link #module_core.storeAndBroadcast `storeAndBroadcast`}
 */
export declare const createStoreAndBroadcast: (provider: Provider) => (trytes: readonly string[], callback?: Callback<readonly string[]> | undefined) => Promise<readonly string[]>;
