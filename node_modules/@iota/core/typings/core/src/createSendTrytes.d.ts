import * as Promise from 'bluebird';
import { AttachToTangle, Callback, Provider, Transaction } from '../../types';
/**
 * @method createSendTrytes
 *
 * @memberof module:core
 *
 * @ignore
 *
 * @param {Provider} provider - Network provider
 *
 * @return {Function} {@link #module_core.sendTrytes `sendTrytes`}
 */
export declare const createSendTrytes: (provider: Provider, attachFn?: AttachToTangle | undefined) => (trytes: readonly string[], depth: number, minWeightMagnitude: number, reference?: string | undefined, callback?: Callback<readonly Transaction[]> | undefined) => Promise<readonly Transaction[]>;
